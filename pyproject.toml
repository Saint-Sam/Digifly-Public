[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "digifly-public"
version = "0.1.0"
description = "Public-facing Digifly notebooks and helper code extracted from the current Digifly master workspace."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "neuprint-python",
    "numpy",
    "pandas",
]

[project.optional-dependencies]
notebooks = [
    "ipython",
    "ipywidgets>=8.1,<9",
    "jupyterlab_widgets>=3,<4",
    "jupyterlab>=4,<5",
    "ipykernel>=6,<7",
    "matplotlib",
    "navis",
    "networkx",
    "openpyxl",
    "plotly",
    "scipy",
    "scikit-learn",
    "tqdm",
    "trimesh",
]
mutation-app = [
    "ipywidgets>=8.1,<9",
    "jupyterlab_widgets>=3,<4",
    "ipykernel>=6,<7",
    "matplotlib",
    "numpy",
    "pandas",
    "pillow",
    "plotly",
    "pyarrow",
    "pyvista",
]

[tool.setuptools]
package-dir = {"" = "Phase 1"}
packages = ["digifly_phase1"]
py-modules = [
    "filter_ids_by_size_and_export_swc",
    "phase1_bridge",
]
