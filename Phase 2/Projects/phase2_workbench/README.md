# Phase 2 Workbench

Canonical app-like Phase 2 notebook project.

Use `Digifly_Phase2_Workbench.ipynb` for presets, validation, run bundles, voltage plots, browser visualization, and the WSL/PyVista mutation-app launch path. Outputs from this project are written under `outputs/`.
